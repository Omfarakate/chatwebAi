/** @type {import('next').NextConfig} */
const nextConfig = {
  // This explicitly tells Next.js to ONLY treat these specific file types as pages/routes
  pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
};

export default nextConfig;